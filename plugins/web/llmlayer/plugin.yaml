name: web-llmlayer
version: 1.0.0
description: "LLMLayer web search + scrape backend. Requires LLMLAYER_API_KEY — sign up at https://llmlayer.ai."
author: NousResearch
kind: backend
provides_web_providers:
  - llmlayer
